#include<iostream>
using namespace std;

void bubblesort(int arr,size){
    for(int i=1;i<n;i++){
        bool swap=false;
        for(j=0;j<n-i;j++){
            if(arr[j]>arr[j+1]){
                swap(arr[j],arr[j+1]);

                bool swap=true;
            }
        }
    }
    if(bool swap==false){
        break;
    }
}

